# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.00.0.1:9030/TEST?useUnicode=true&characterEncoding=UTF-8
    username: root
    password:

logging:
  level:
    root: debug
