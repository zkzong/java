<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.mp.org.example.UserMapper">

    <select id="selectAll" resultType="entity.mp.org.example.User">
        select * from user ${ew.customSqlSegment}
    </select>

    <select id="selectUserPage" resultType="entity.mp.org.example.User">
        select * from user ${ew.customSqlSegment}
    </select>

</mapper>